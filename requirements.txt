# Core data processing - these are already installed
pandas
numpy

# HTTP and parsing - these are already installed
requests
beautifulsoup4
html5lib

# Database - already installed
sqlalchemy

# Date handling - already installed
python-dateutil

# Rather than using alpha_vantage which requires aiohttp (causing build issues),
# we'll implement a simple REST client using requests directly

# Optional packages - comment these out until Python 3.13 compatibility improves
# yfinance
# plotly
# ta
# great-expectations
# lxml
